Imges of danger pictograms can be gotten from <https://www.psm.admin.ch/images/psm/GHS09.gif>. Note that you can get these images by the identifier.

I think the filter panel is much too long -- could you reduce it in size by using the text search element more often?

Also, can you use oblique's autocomplete where it underlines the matching parts?

Also, the labels of some codes (e.g. labelling) are waaaay too long (200 characters or so) -- can you fix this? Trunkate or something like that?

Also, make sure to order the filters by relevance:

1. crop
2. pest
3. classification
4. ...

I think the "chapters" in between the filters are not necessary.

Essentially, probably all filters (in the standard search) can be modelled as dropdown elements, either multiselect or text search with autocomplete.

Pls. default items per page at 12